{
  "name": "@mobile-copilot/relay-server",
  "version": "0.2.0",
  "description": "Cloud relay server for Mobile Copilot — bridges VS Code extensions and mobile clients via WebSocket rooms",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.js",
    "start": "node dist/index.js",
    "dev": "node --watch src/index.ts"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
